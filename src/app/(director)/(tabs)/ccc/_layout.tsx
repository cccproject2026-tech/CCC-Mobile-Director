import { Stack } from 'expo-router';

export default function CCCLayout() {
    return (
        <Stack
            screenOptions={{
                headerShown: false,
            }}
        />
    );
}
