import { Stack } from "expo-router";

export default function CourseCompletedLayout() {
    return (
        <Stack
            screenOptions={{
                headerShown: false,
            }}
        />
    );
}
